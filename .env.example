# Keygen API Configuration
# Replace these values with your actual Keygen instance details

# Keygen API URL (use your instance URL)
# Note: Also set as NEXT_PUBLIC_KEYGEN_API_URL during build
KEYGEN_API_URL=https://api.keygen.sh/v1
NEXT_PUBLIC_KEYGEN_API_URL=https://api.keygen.sh/v1

# Your Keygen Account ID
# Note: Also set as NEXT_PUBLIC_KEYGEN_ACCOUNT_ID during build
KEYGEN_ACCOUNT_ID=your-account-id-here
NEXT_PUBLIC_KEYGEN_ACCOUNT_ID=your-account-id-here

# Admin credentials for authentication (runtime only)
KEYGEN_ADMIN_EMAIL=your-email@example.com
KEYGEN_ADMIN_PASSWORD=your-secure-password

# Optional: Set to 'production' for production builds
NODE_ENV=production
